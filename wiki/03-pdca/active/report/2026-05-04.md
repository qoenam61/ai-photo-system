# 세션 리포트 — Phase 5 단축 활성화 + Layer 5 신규

**날짜**: 2026-05-03 ~ 2026-05-04
**Mode**: DEEP-ARCHITECT (도메인 안전 영역 우회)
**사용자 결정**: 🅒 + 자동승인 — 14일 게이트 단축 + TRASH/dedup 즉시 진행 + 파이프라인 검증·개선·실제 동작 확인

## 진행 단계

### Phase A — 진단 (PLAN)
- 컨테이너 health 확인 (photo-classify·immich·trading_postgres·n8n·ollama 모두 healthy)
- classify-service v3.13 VISION_MODEL_CHAIN=groq,qwen 정상
- iPhone 업로드 8,476장 / classification 12,168장 / pending 0 — 분류 완료 사실상 100%
- poetry venv 3.14 → 3.12 복구, httpx 등 의존성 재설치
- `phase5_activation.py` readiness 자동 점검 정상화
- **중대 발견**: `core/service/cleanup_service.py` 미구현, cleanup_queue INSERT 코드 부재, HDD `Path.unlink()` 호출 부재 — Layer 5 워커가 존재하지 않음

### Phase B — 설계 (DESIGN)
- 정식 TC 작성: `wiki/03-pdca/active/report/TC-phase5-fastlane.md`
- 단축 정책 SSoT 갱신: CLAUDE.md "Phase 5 단계적 활성화 절차 (단축 결정)" 추가
- 분담 결정: `core/service/cleanup_service.py`는 enqueue만 (컨테이너 OK), HDD unlink는 호스트 워커 `scripts/cleanup_run.py`로 분리 — `/storage:ro` 보호선 유지

### Phase C — 구현 (DO)
| 파일 | 종류 | 설명 |
|---|---|---|
| `core/service/cleanup_service.py` | 신규 | enqueue_cleanup, fetch_processable_queue, mark_processed, cancel_queue_item |
| `core/api/classify_server.py` | 수정 | `POST /cleanup_enqueue` 엔드포인트 추가 |
| `scripts/cleanup_run.py` | 신규 | 호스트 워커 — verify HTTP + HDD unlink + audit INSERT |
| `scripts/maintenance.sh` | 수정 | `[5/5] Layer 5 HDD cleanup` 30분 cron 추가 |
| `scripts/verify_backup_full.py` | 보강 | 호스트 DSN 호환을 위해 HTTP 기반 리팩터, processed 자산 자동 제외 |
| `scripts/_inventory/phase5_ready.flag` | 신규 | progressive_limit 활성 |
| `wiki/03-pdca/active/report/TC-phase5-fastlane.md` | 신규 | 정식 TC + ACTUAL |

### Phase D — 검증 (CHECK)
- `/cleanup_enqueue` 시범 20장 등록: dedup 5 (grace=0) + TRASH 15 (grace=24h) → `enqueued=20`
- `cleanup_run.py --limit 5` (dry-run) → 5장 모두 verify PASS, host path 정상
- `cleanup_run.py --limit 5 --no-dry-run` → 5장 실삭제 (audit#2~6, 11.5 MB)
- 잔여 dedup 40장 추가 등록 + 처리 → audit#7~46, 98.5 MB
- **누적 110.1 MB 회수, audit success 45/45**
- HDD 파일 미존재 확인 (`no matches found`)
- TRASH safe pool (auto_screenshot + llm_*) 588장 추가 등록 (24h grace) → 누적 queued 648장
- sample 500장 verify_backup_full **100% PASS** (3.5/s)
- 전수 12,205장 verify 백그라운드 진행 중 (~1시간)

### Phase E — 정책 결정 (보류)
- **`auto_short_video` 562장** (TRASH iphone_upload + legacy): LLM 미관여 길이 휴리스틱이라 false-positive 위험. 사용자 검토 받기 전 cleanup_queue 등록 X.
- **MEMORY- 32장**: v3.13 정책 "TRASH 외 모든 등급 HDD 영구보존" 준수 — HDD 영구삭제 X.
- **iOS Shortcut [Show Notification] → [Delete Photos] 교체**: 사용자 측 작업. 가이드는 `runbooks/layer6_ios_shortcut.md`에 이미 존재.

## DB 정합 (2026-05-04 00:30)

| 항목 | 값 |
|---|---|
| `photo.classification` | 12,205장 (Immich active 12,205장) |
| `photo.cleanup_queue` total | 648 (45 processed + 603 grace 대기) |
| `photo.cleanup_audit` success | 45 |
| 회수 디스크 | 110.1 MB |
| 첫 grace 만료 | 2026-05-04 21:27 (24h 후) |

## 이슈

1. SDLC 표준 스크립트 부재
   - `scripts/deploy-verify.sh` / `scripts/doc-consistency-check.sh` / `scripts/session-report.sh` 미작성. 수동 검증으로 갈음. 별도 backlog.
2. `core/service/__init__.py` 주석에 `cleanup_service.py` 언급되어 있었으나 실제 파일 없었음 — 현재 신규 작성으로 정합.
3. `verify_backup_full.py` 호스트 DSN 비호환 → HTTP 리팩터로 해결.

## 사용자 측 후속 작업

1. **iOS Shortcut 활성화** (`runbooks/layer6_ios_shortcut.md` 참조)
   - Shortcut `Photo Cleanup (jw.son)`의 `[Show Notification]` → `[Delete Photos]` 교체
   - 첫 주 limit=20 (Shortcut에서 `cleanup_candidates?limit=20` 호출)
   - iCloud Recently Deleted 30일 안전망 의존
2. **`auto_short_video` TRASH 562장 검토 결정**
   - 보존 / 일괄 삭제 / 등급 재분류 — 사용자 결정 필요
3. **24h 후 자동 처리 모니터링** (2026-05-04 21:27~)
   - `maintenance.sh` 30분 cron이 `cleanup_run.py --limit 100 --no-dry-run` 자동 호출
   - `tail -f scripts/_inventory/maintenance_$(date +%Y%m%d).log` 실시간 확인 가능

## 다음 세션 진입 가이드

- `wiki/03-pdca/active/report/TC-phase5-fastlane.md` ACTUAL 갱신 (전수 verify 결과 + grace 만료 후 처리 결과)
- `auto_short_video` 사용자 결정 후 추가 등록 또는 보존
- BEST/EVENT 등급 정책은 변경 X (HDD 보존 유지)

## Backlog 정리 + 표준 스크립트 신규 — 2026-05-04 08:09:15 KST

| 항목 | 값 |
|---|---|
| photo.classification | 12205장 |
| cleanup_queue | total=648 processed=45 |
| cleanup_audit success | 45 |
| 누적 회수 | 110.0948152542114258 MB |
| 미커밋 파일 | 4 |


## 표준 스크립트 검증 재실행 — 2026-05-04 08:09:30 KST

| 항목 | 값 |
|---|---|
| photo.classification | 12205장 |
| cleanup_queue | total=648 processed=45 |
| cleanup_audit success | 45 |
| 누적 회수 | 110.0948152542114258 MB |
| 미커밋 파일 | 5 |

